// schema namespace (en). Schema #1 validator violation messages (shown to users in the downgrade bar / details). AI-drafted, Colin to review.
module.exports = {
  blockNoStyle: 'Block-level elements can’t carry a style attribute',
  pInContainerNoBlock: 'A <p> inside a container can’t contain block-level elements',
  containerMultiPara: 'A container (callout/quote) allows only multiple <p> + inline, not lists or other blocks',
  containerInlineNoBlock: 'Inline elements inside a container (callout/quote) can’t hide block-level elements',
  phrasingOnlyNot: '{what} can only hold inline content, not {tag}',
  phrasingInlineNoBlock: 'Inline elements in {what} can’t hide block-level elements',
  ulNoText: 'Direct children of ul/ol must be <li>, not bare text',
  ulLiOnly: 'Direct children of ul/ol must be <li>',
  todoChecked: 'data-checked must be true/false, got: {value}',
  liContent: 'An <li> may only contain inline content + a trailing sublist',
  liInlineNoBlock: 'Inline elements inside an <li> can’t hide block-level elements',
  noMergeCells: 'Merged cells (colspan/rowspan) are not allowed',
  cellPhrasingNot: 'A table cell can only hold inline content, not {tag}',
  cellInlineNoBlock: 'Inline elements inside a table cell can’t hide block-level elements',
  noCaption: '<caption> is not allowed',
  noColgroup: '<colgroup> is not allowed',
  noTfoot: '<tfoot> is not allowed',
  theadOneRow: 'A table header may have at most one row',
  tableRect: 'A table must be rectangular (every row has the same number of cells)',
  figureContent: 'A figure may only contain <img> + an optional <figcaption>',
  figureImgCount: 'A figure must contain exactly one <img>, got {count}',
  detailsSummaryCount: 'A toggle must have exactly one <summary>, got {count}',
  summaryFirst: '<summary> must be the first child of details',
  bareDivNotBlock: 'A bare <div> isn’t a valid block (only div.ws-callout)',
  notInBlockSet: '{tag} isn’t in the Schema #1 block set (h5/h6/section/… don’t conform)',
  leafNoBlock: '{tag} is a leaf text block and can’t contain block-level elements',
  noMetaHttpEquiv: 'meta http-equiv is not allowed (refresh and similar redirect hijacks)',
  headStyleOnly: 'head only allows the Schema baseline <style data-ws-schema-css>',
  noBase: '<base> is not allowed (it rewrites all relative URLs in the document)',
  noLink: 'External <link> is not allowed',
  headNotAllowed: 'head doesn’t allow {tag}',
  noScript: 'Scripts are not allowed',
  noTemplate: '<template> is not allowed (its content escapes the safety scan and never conforms to the Schema anywhere)',
  noEventAttr: 'Inline event attribute {attr} is not allowed',
  unsafeHref: 'Unsafe link href',
  unsafeSrc: 'Unsafe src (js/vbscript/file/blob, or a data: that isn’t image/svg)',
  unsafeXlink: 'Unsafe xlink:href',
  unsafeSrcset: 'Unsafe srcset',
  dangerStyleValue: 'Unsafe style value not allowed (position:fixed/url()/expression, etc.)',
  topText: 'Bare text isn’t allowed at the top level; wrap it in a block',
  pageCssUnparseable: 'Page layout (@page) couldn’t be parsed; opening as a flowing document',
};
