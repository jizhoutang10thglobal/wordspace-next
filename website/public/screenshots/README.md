# Screenshots

Placeholder directory for product screenshots (app UI, Copy Prompt toast, PagedView).
Colin will drop real screenshots here after v1. The landing page currently uses
inline SVG for the Copy Prompt demo panel instead of a raster image.
