export function AutoUpdate() {
  return (
    <section className="section features" id="auto-update">
      <div className="container">
        <article className="feature feature--solo">
          <div className="feature__num">自动更新</div>
          <h2 className="feature__title">始终保持最新</h2>
          <p className="feature__body">
            每次打开 wordspace，它都会在后台自动下载最新版本，并提示你一键安装——
            不用手动检查，也不用重新下载。
          </p>
        </article>
      </div>
    </section>
  );
}
