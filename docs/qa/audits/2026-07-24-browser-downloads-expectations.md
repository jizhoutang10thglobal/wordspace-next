# 浏览器下载 · 行为预期清单(判官第一步,采证前,70 条)
A 触发落盘(1-5):CD attachment→自动落系统下载夹不弹框(有意分歧勿报)/<a download>用其名/不可渲染资源触发/字节完整能开/落系统下载夹跟随本地化。
B uniquify(6-10):重名扩展名前插 (n) 绝不覆盖(P1)/并发同名各拿序号/无扩展名末尾追加/多段扩展名插入点(待核)/清空记录后同名仍 uniquify 不覆盖磁盘旧文件。
C 状态机(11-17):completed 可开/取消 canceled 半截清理(待核)/网络断 failed 给重试/退出在途转 interrupted 不续传(有意分歧)/文件被删 fileMissing 置灰/终态可移除/重试=新条目置顶再 uniquify。
D 进度环(18-24):在途叠环/聚合%=Σ收/Σ总/单条先完成环不回退(硬点)/计数徽标/全完环消失/无 Content-Length 表现(待核)/中途新增下载分母变化会不会回退(待核)。
E popover 列表(25-35):点图标开列表非整页/文件名中段截断+全名/进行中=取消/完成=打开+访达+移除/可重试=重试+移除/fileMissing 置灰仅移除/清空记录只清终态在途留/空态有引导/打开=手动系统 app(不自动开红线)/访达=reveal 高亮。
F popover 位置(36-37):锁侧栏宽不盖网页(硬点)/锚下载图标附近。
G 关闭(38-41):veil 点侧栏关/Esc 关/图标 toggle 关/关后焦点合理网页恢复。
H toast 择位(42-49):侧栏开=侧栏内紧凑锚图标下不顶网页/侧栏收=over-web/开始 neutral/完成 success+显示/失败 danger/启动 interrupted 计数条/进行中不刷 progress toast(有意分歧)/自动消失。
I 持久化(50-54):只存元数据/重启记录在/CAP100 挤终态不挤在途/重启 completed 可开、删了 fileMissing/重启 downloading→interrupted 不假活。
J 右键(55-57):存图片走同管线/链接另存为同管线/右键也不弹框(不一致则待核)。
K 安全(58-61):砍对话框会不会被恶意网页狂写塞盘(待核,砍对话框的攻击面)/下载完绝不自动打开可执行(P1 红线)/文件名含 ../绝对路径分隔符被无害化不穿越(P1)/非法字符无害化。
L 边界(62-66):超长名截断保扩展名/磁盘满失败给重试不崩/连发 N 各自独立 uniquify 计数=N/亮暗全清晰/中文名+RFC5987 编码正确(需真机手测)。
M 相邻(67-70):关触发标签在途继续跑/切标签记录进度全局不变/popover 开切标签 veil 关/侧栏收→展开状态一致。

有意分歧勿报:1不弹框/14不续传/25无整页/34不自动打开/48进行中无 progress toast。
待核:9多段扩展名/12取消残留/23无CL进度/24分母回退/57右键弹框/58无手势滥用/60穿越/62超长截断/67关标签在途。
需真机:66中文名编码。安全红线:34/59不自动打开、60穿越、6不覆盖。
