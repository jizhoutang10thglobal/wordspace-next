{
  "spec": "release-badge",
  "title": "状态栏发布徽标的可见验收",
  "note": "由人(Colin)拍板、实现 AI 不许改（.github/CODEOWNERS 锁）。验 #status-bar 的 textContent 含固定徽标 'Shipped by the pipeline'。判据可证伪：空状态栏 / 没加徽标都判红；变异自检清空 #status-bar 文本时必翻红。徽标文案固定，实现须原样落这串。",
  "launch": {
    "main": "src/main.js",
    "waitFor": "#status-bar"
  },
  "steps": [
    { "snapshot": "shown" }
  ],
  "checks": [
    {
      "id": "badge-shown",
      "desc": "状态栏显示固定发布徽标 'Shipped by the pipeline'",
      "selector": "#status-bar",
      "metric": "textContent",
      "states": { "shown": { "contains": "Shipped by the pipeline" } }
    }
  ]
}
