# Changelog

> **本文件是用户可见更新说明的正本**，同一段内容流向三处：GitHub Release 页顶部（`---` 之上）、
> App 内更新面板（自动解析 Release 说明）、官网 [wordspace.ai/changelog](https://wordspace.ai/changelog)（自动渲染本文件）。
> **文案规范见 `docs/releasing.md`「Changelog 文案规范」**——用户视角短句、新增/改进/修复分组、
> 不写根因与内部机制、开发侧改动不进本文件。
> 版本号：tag 即版本真源（`git tag vX.Y.Z && git push origin vX.Y.Z` 触发签名发版），带新功能 bump 0.1，纯修复 bump patch。
> 历史版本（v0.0.1–v0.6.6）为 2026-07-12 回溯补写；全量条目 2026-07-16 按新规范重排。

## v0.10.4 — 2026-07-17

**PDF 可以默认用 Wordspace 打开了，外加三个日常修复：更新弹窗不再盖白网页、「重启安装」二修、标签不再误标「工作区外」。**

- **PDF 默认打开可以设成 Wordspace 了**：macOS「打开方式 → 全部更改」的候选列表里现在有 Wordspace（此前安装包没声明 .pdf 文档类型，列表里根本不出现）；设为默认后双击 PDF 直接在应用内查看器打开。
- **更新弹窗不再把网页盖白**：网页标签（比如 Gmail）开着时弹出「发现新版本」，内容区会整个变白——现在弹窗背后垫了页面快照，网页内容保持可见。
- **「重启安装」二修**：v0.10.1 修过一次但没修干净——点了之后窗口消失、app 却赖在 Dock 里，更新装不上。这次找到真根因（退出信号的监听挂错了对象、从来没生效过），把退出动作直接放到按钮上。老规矩：装上本版的这一次更新仍走旧更新器（可能还要手动退出一次），**从本版起的下一次更新**才是真正的一键重启。
- **标签不再误标「工作区外」**：先打开一个单独文件、再把它所在的文件夹添加进工作区，标签页里这个文件此前会一直带着 ↗「工作区外」标记（Wendi 报的），还会因为身份错乱出现同一文件两条标签。现在添加文件夹时自动收编；老版本已经留下的错误标签，升级后启动即自愈，无需手动处理。
- **侧栏英文栏标统一大写**：英文界面下「Bookmarks」此前混排大小写，与 PINNED / TABS / FILES 不一致（中文界面无此问题），已归一。
- 开发侧：外部标签收编挂到全部「树内容到货」点（含简化模式逐层加载）；两条 e2e 新门（操作流收编 + 启动自愈）先红后绿 + 逐点变异复核；PDF 文档类型声明有打包层单测兜底；侧栏四分区栏标排版签名加 e2e 一致性门。

## v0.10.3 — 2026-07-16

**超大文件夹能正常用了（简化模式），外加整个 app 支持中英文切换。**

- **超大文件夹进「简化模式」**：上一版（v0.10.2）让巨型文件夹不再卡死但打不开内容；这一版能**正常浏览**了——超过 15 万个项目的文件夹自动进入简化模式（根上带徽标），秒级打开、点开哪层加载哪层，照常打开和编辑里面的文档，重启后依旧秒开。实测整个用户目录（约 190 万个项目）从选择到可浏览约 2 秒。
- **简化模式下的取舍（有提示，不静默）**：侧栏筛选只搜浏览过的部分；快速打开（Cmd+P）暂不覆盖这类文件夹；外部改名的标签跟随降级为按路径匹配。
- **大文件夹的后台负担大幅下降**：磁盘变化只重读你浏览过的目录，高频写入的大文件夹不再触发全量重扫；@链接菜单不再钻进 node_modules / .app 包内部（更快也更准）。
- **中英文切换**：设置页新增「语言」——跟随系统（默认）/ 中文 / English，切换后整窗重载生效；菜单栏、对话框、提示等全量覆盖。英文文案为初版，术语仍在打磨中。
- 开发侧：文件树构建消除 O(M²)（15 万条目宽树 68s → 0.6s）；诊断面板补目录数与载荷指标；i18n 三道防漂移门接入 CI；两轮对抗审查（真实 191 万条目家目录重放全过）。

## v0.10.2 — 2026-07-16

**界面「零缝隙」沉浸模式（对标 Arc），外加超大文件夹不再把 app 冻死。**（补账：本段与 Release 页同步）

- **系统标题栏没了（macOS）**：窗口顶上那条「Wordspace Next」标题栏取消，红绿灯按钮融进侧栏顶部——窗口从第一个像素起就是内容；侧栏顶部区域可以拖动窗口。（Windows 暂保持原窗框）
- **收起侧栏 = 完全沉浸**：收起后左侧不再留任何窄条或按钮，内容（尤其网页标签）铺满整个窗口，红绿灯也跟着隐藏——和 Arc 一样「所有的地方都可以隐藏」。
- **鼠标滑到最左边缘，侧栏悬浮滑出**：不用展开也能快速用一下侧栏（点文件、切标签），鼠标移开自动缩回；`⌘\` 快捷键照旧；第一次点收起按钮会弹气泡教你快捷键。
- **超大文件夹止血（P0）**：选了个巨大的文件夹（比如整个家目录、根目录）不再把 app 直接冻死——添加高危路径（`~`、`/`、`/Users`、整个卷）前先劝导确认；扫描有条目预算，超限的文件夹标「过大」而不是无限扫下去；启动时逐个文件夹加载、不再因为上次的大文件夹卡在空白；新增「管理文件夹…」逃生门，树挂了也能把问题文件夹移掉。

## v0.10.1 — 2026-07-16

更新体验三连修。

### 修复

- **更新**：点「重启安装」现在真的会重启并装上（此前需要手动退出 app 才生效）
- **更新**：不再每次都要密码/指纹——首次提示一次性修复，之后永久免密
- **更新**：下载进度面板不再闪烁
- 注：装上本版的那一次更新仍走旧更新器；新体验从本版之后的下一次更新开始

### 改进

- **快捷键**：主要按钮悬停显示快捷键提示，新用户有一次性教学气泡
- **侧栏**：最小宽度收紧，拖到最窄不再裁掉顶排图标

## v0.10.0 — 2026-07-15

能往文档里插图片了。

### 新增

- **图片**：三种方式插入——从访达拖入、粘贴截图（⌘V）、斜杠菜单「/图片」
- **图片说明**：选中图片可在下方加一行说明文字
- **「视图」菜单**：⌘\ 聚焦/切换侧栏、⌘R 刷新网页标签

### 改进

- 插入的图片自动压缩、方向摆正，内联存进文档——依然是自包含的单个文件
- 图片整块选中，可删除、拖拽重排
- 剪贴板同时有文字和图片时优先粘文字

## v0.9.1 — 2026-07-15

### 修复

- **深色模式**：侧栏左下角 logo 反白显示（此前黑 logo 埋进暗底看不见）

## v0.9.0 — 2026-07-15

深色模式来了。

### 新增

- **深色模式**：浅色 / 深色 / 跟随系统三态，整个界面全量变暗，选择跨重启记住
- 文档内容智能变暗：浅色文档反色、图片和本就深色的文档不动；只影响显示，存盘内容不变
- 支持深色的网站自动跟随变暗；PDF 导出始终保持文档原本的浅色

### 改进

- **侧栏**：文件夹也能拖拽移动了（此前只有文件）；吸顶的文件夹标题可作拖放目标
- **标签区**：不同文件夹里的同名文件显示所属文件夹以便区分

### 修复

- 连删多个文件时每个都可单独撤销；撤销删除后置顶状态一并恢复
- 在外部删掉正打开且有未保存改动的文档时，弹「保存到哪里」救回改动
- 网页加载失败后，换网址或刷新能真恢复，不再卡死在错误页
- 改名不再叠出「.md.html」双后缀；文件夹展开/收起状态重启后记住
- 外置盘拔了再插回、或文件夹路径改回来时自动恢复，不用手点「重新定位」
- **地址栏**：带冒号的搜索词（如「note:开会」）不再被拦；打字中切标签不再误导航残留文字
- **收藏**：文件夹重名自动加序号；书签导出再导入不再翻倍；文档标签去掉无效星标

## v0.8.4 — 2026-07-14

链接能跨文件夹空间了。

### 新增

- **跨空间互链**：并排打开多个文件夹时，@ 搜索、拖拽建链、悬停预览、断链修复都跨文件夹工作；改名/移动后其他空间的引用自动跟随（可撤销）

### 修复

- **浏览器**：不再反复弹人机验证
- **大文件夹**：磁盘变化只扫变化的部分，消掉卡顿；添加大文件夹时有加载反馈
- 点标签页定位到文件时不再乱滚文件树
- Windows / 云盘同步产生的隐藏垃圾文件（.DS_Store 等）不再进文件树
- 基础编辑模式去掉多余的悬停蓝色虚线框

## v0.8.3 — 2026-07-13

文档互链全套收官，还能设为 macOS 默认浏览器。

### 新增

- **互链维护**：失效链接红线提示并给修复候选；改名/移动后所有引用自动跟着改（可撤销）；标题下显示「N 篇文档链接到这里」；删除被引用的文件先警告
- **默认浏览器**：可在系统设置把 Wordspace 设为 macOS 默认浏览器，其他 app 点开的链接直接开成标签

### 改进

- **侧栏**：收藏区视觉归化，与其他栏目对齐

## v0.8.0 — 2026-07-12

### 新增

- **文档互链**：@ 提及快速插入、悬停预览、从侧栏拖文件成链接，改名/移动后链接自动跟随
- **分页文档**：Word 式 A4 分页排版、页面设置、导出 PDF 带页码

## v0.7.0 — 2026-07-12

Wordspace 现在也是一个浏览器：网页和文档共用同一套侧栏、地址栏和标签。

### 新增

- 地址栏上网或直接搜索，自动补全（开着的标签 / 收藏 / 历史）
- **收藏夹**（与 Chrome / Safari / Firefox / Edge 互导）、历史记录、起始页
- 页内查找（⌘F）、缩放、右键菜单、会话恢复、全套标签快捷键
- 网页标签与文档标签混装、置顶、拖拽

### 修复

- 新标签加载中闪回旧文档等一批边角问题

## v0.6.6 — 2026-07-11

### 修复

- **打开桌面/大文件夹不再卡**：文件树跳过应用包（.app）和依赖目录（node_modules 等）

## v0.6.5 — 2026-07-10

### 新增

- **性能诊断模式**（菜单「性能诊断…」）：卡顿时自查耗时、一键录 CPU Profile 发给我们排查

## v0.6.4 — 2026-07-09

### 修复

- **数据安全**：文档里点相对链接后自动保存可能写错文件——已收口，链接统一应用内打开
- 关闭标签后激活相邻标签（原来跳到最后一个），文件树不再乱滚
- 多文件夹工作区渲染提速（展开/折叠不再整树重建）

## v0.6.3 — 2026-07-08

### 新增

- **多文件夹工作区**：同时打开多个文件夹，侧栏并排显示；嵌套自动合并；文件夹失联显示灰态可重新定位
- 文件可在文件夹之间直接拖拽移动

## v0.6.2 — 2026-07-06（发版流水线中断，无安装包；改动随 v0.6.3 到达用户）

- 「纸方墨圆」设计语言进 app；深层文件夹合并显示（compact folders）；滚动时祖先文件夹吸顶

## v0.6.1 — 2026-07-05

### 修复

- **全面 bug sweep**：修掉约 40 个问题，含两个严重项（删除线掉出编辑器、跨块删除损坏表格）和一批丢数据边角
- macOS 上 .md 文件可绑定 Wordspace 双击打开
- 后台未保存的临时文档关闭时也弹确认

### 改进

- 默认排版观感升级（标题/段落/列表间距对齐 Notion 水准）

## v0.6.0 — 2026-07-03

### 新增

- **Markdown 支持**：.md 文件直接打开、编辑、保存，与 HTML 文档同一个编辑器
- **AI 接入**：复制 Prompt 或安装 Skill，让你的 AI 会写 Wordspace 文档

### 改进

- macOS 关窗改「隐藏驻留」：关窗后留在 Dock，再点秒开，标签与未保存内容都在
- 从访达双击打开文件时，文件树自动展开定位

## v0.5.0 — 2026-07-02

### 新增

- **格式校验**：打开文件自动判别——标准格式走块编辑，野生 HTML 自动降级基础编辑并提示，不再「编辑坏文件损坏内容」
- **临时文档**：新建文档先编辑后保存，保存时再选位置
- 侧栏真收起（⌘\）、⌘P 快速打开（模糊搜文件名）

### 改进

- 导出收敛为单按钮 PDF 导出（所见即所得）；新增另存为与右上角 ⋯ 菜单

## v0.4.5 — 2026-06-30

### 修复

- app 未启动时双击 .html 文件，打开后侧栏缺对应标签

## v0.4.4 — 2026-06-30

- 置顶区标签加 ×，可直接移出置顶

## v0.4.3 — 2026-06-28

### 新增

- 工作区外的文件也能进标签页（带 ↗ 标记，重启恢复）
- 文件树实时跟随磁盘：外部改名/移动/删除自动同步，不用手动刷新

## v0.4.2 — 2026-06-26

### 修复

- 一批试用反馈：空置顶区占位、「打开」支持任意文件类型（图片/PDF 应用内预览）、查看器顶栏对齐

## v0.4.1 — 2026-06-26

- 标签页与置顶打磨：拖拽排序/跨区、重启恢复、未保存圆点等

## v0.4.0 — 2026-06-25

### 新增

- **本地文件夹工作区**：打开文件夹即工作区，文件树浏览/新建/重命名/移动/删除（可撤销）/筛选，标签页管理打开的文档

## v0.3.6 — 2026-06-24

### 修复

- **文档保真**：自带样式的 HTML 按原样显示，不再被强行套排版

### 新增

- 编号列表 / 待办列表 + markdown 快捷触发（「1.」「[]」等）+ Tab 嵌套
- wordspace.ai 品牌 logo 上线；官网加 Intel Mac 下载

## v0.3.5 — 2026-06-23

- **支持 Intel Mac**：同时发 Apple Silicon 与 Intel 双架构安装包

## v0.3.4 — 2026-06-23

- 菜单加「报告问题 / 反馈…」入口

## v0.3.3 — 2026-06-23

- **PDF 导出**上线；修复空块连按 Enter 不换行

## v0.3.2 — 2026-06-22

### 修复

- 一批试用反馈：包裹容器可编辑、外部改动自动重载、长文件名截断、跨块拖选、最小窗口尺寸

### 新增

- 保存成功「✓ 已保存」反馈；触控板捏合缩放

## v0.3.1 — 2026-06-18

### 修复

- 整篇被 div 包裹的文档点不进去编辑
- 斜杠菜单在空块插列表得到空列表

## v0.3.0 — 2026-06-18

### 新增

- **编辑器换代**：Notion 式块编辑器（单击即编、Enter 新建块、斜杠菜单、块菜单）

## v0.2.0 — 2026-06-17

- 新界面上线（侧栏/顶栏布局）+ 第一代画布编辑器；官网迁入并改极简单页

## v0.1.3 — 2026-06-14

- **Windows 支持**：安装包 + 文件关联（双击 .html 打开）

## v0.1.2 — 2026-06-14

- 内部改动（UI 原型体系入仓），无用户可见变化

## v0.1.1 — 2026-06-14

- 安装包文件名固定，官网下载链接永久有效

## v0.1.0 — 2026-06-13

- **第一个可用编辑器**：从「壳」变成能编辑文档的 app

## v0.0.4 — 2026-06-11

- 仓库转正为 Wordspace Next 正式主仓；发布链修复

## v0.0.3 — 2026-06-11

- 最早的文本输入与基础编辑能力

## v0.0.2 — 2026-06-10

- 状态栏发布徽标 + 显式更新弹窗

## v0.0.1 — 2026-06-09

- 最小 Electron 骨架 + **macOS 签名发布/自动更新流水线**
